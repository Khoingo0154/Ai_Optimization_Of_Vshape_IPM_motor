# Motor Optimizer v5.2 (Fix Lan 3) - dependencies
# Install:  pip install -r requirements.txt
# Python 3.10+ (tested on 3.13, 3.14)

numpy>=1.24
pandas>=2.0
openpyxl>=3.1
PyYAML>=6.0
pywin32>=305
scikit-learn>=1.3
matplotlib>=3.7
psutil>=5.9

# Optional: only needed for Method A (PyAEDT-based Ansys integration).
# Method B (win32com ActiveX) does not require it.
# pyaedt>=0.6.0
